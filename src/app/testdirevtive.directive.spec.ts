import { TestdirevtiveDirective } from './testdirevtive.directive';

describe('TestdirevtiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TestdirevtiveDirective();
    expect(directive).toBeTruthy();
  });
});
